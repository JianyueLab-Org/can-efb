# build output
dist/

# generated types
.astro/

# dependencies
#
# **不带斜杠。** `node_modules/` 只匹配目录，而把仓库当 worktree 用时这里常常是一
# 条指向主检出的**符号链接** —— 那种写法匹配不到它，于是 `git add -A` 会把链接本身
# 提交上去。提交之后在 CI 上它指向一个不存在的路径，`bun install` 直接 ENOENT，而
# 本地一切正常。
node_modules

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# environment variables
.env
.env.production

# macOS-specific files
.DS_Store

# jetbrains setting folder
.idea/

# Claude Code 的本地状态。要紧的是 .claude/worktrees/ —— 它在这个仓库里再检出
# 一份工作副本（连 node_modules 一起，轻易上万个文件）。不忽略的话，把本仓库当
# submodule 的 CeruleanAviationNetwork 每次 git status 都报「子模块有未跟踪内容」，
# 而那既不是改动也不是待推送的东西。
.claude/
