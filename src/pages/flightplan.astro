---
import AppLayout from "@/layouts/AppLayout.astro";
import PageHeader from "@/components/PageHeader.astro";
import FlightPlan from "@/components/FlightPlan.vue";
import { getLocale, getMessages, useTranslations } from "@/lib/i18n";

const locale = getLocale(Astro.cookies);
const t = useTranslations(locale, "efb");
const title = t("pages.flightplan.title");
const description = t("pages.flightplan.description");
---

<AppLayout title={title} description={description}>
  <PageHeader title={title} description={description} />
  <FlightPlan messages={getMessages(locale, "efb")} client:load />
</AppLayout>
